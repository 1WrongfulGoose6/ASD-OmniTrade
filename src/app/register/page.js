import React from "react";
import RegisterForm from "@/components/RegisterForm";

export const metadata = {
  title: "Register - OmniTrade",
  description: "Create your OmniTrade account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}